# 12주차 교재 개편본 — 사용법

## 들어 있는 것

```
pages/          w12-0-overview / w12-1-intro / w12-2-advanced / w12-3-lab  (전면 재작성)
assets/         w12_*.png 12장 (전부 새로 제작, 구 그림 4장은 삭제 대상)
TOC.md          12주차 제목 4개가 갱신된 최신본
README.md       저장소 안내 (12주차 ✅ 반영)
sync_toc_py.txt → sync_toc.py 로 이름을 바꿔서 쓰십시오
lab12/          이 주차의 모든 숫자를 만든 실험 코드·결과 JSON
```

## 저장소에 반영하는 순서

```bash
cp pages/*.md   <저장소>/pages/
cp assets/*.png <저장소>/assets/
cp TOC.md README.md <저장소>/

# 구 12주차 그림 4장을 지웁니다 (더 이상 참조되지 않습니다)
#   ※ w12_p1_fedavg_02 와 w12_p1_noniid_03 은 이름이 같아 새 그림으로 덮어써집니다
cd <저장소>/assets
rm -f w12_p1_centralized_vs_fl_01.png w12_p2_lora_peft_04.png \
      w12_p2_privacy_05.png w12_p3_fedavg_sim_06.png

cd <저장소> && python3 sync_toc.py          # 통과해야 push
git add -A && git commit -m "docs: 12주차 개편 (기울기 역복원·프라이버시 실측)" && git push origin main
```

## lab12 재현

```bash
pip install torch torchvision numpy opacus
python3 run_fed.py     # 비-IID · 통신량 · 희소화        → fed.json    (약 40분)
python3 run_patho.py   # FedAvg 논문 §3 의 병리적 분할   → patho.json  (약 2분)
python3 run_leak.py    # 기울기 역복원 · 배치 · 클리핑   → leak.json   (약 1분)
python3 run_dp.py      # 차등 프라이버시 정확도·ε        → dp.json     (약 13분)
python3 student.py     # 3교시 학생용 코드
python3 make_figs.py   # 그림 12장 재생성 (SVG → PNG)
```

MNIST 는 `torchvision` 이 자동으로 내려받습니다(약 10 MB). **GPU·기기 여러 대 불필요.**
`run_fed.py` 만 오래 걸리고, **2교시의 핵심 실험(`run_leak.py`)은 1분이면 끝납니다.**

---

## 이번 주차의 핵심 — 나눗셈 한 번

선형층 $z = Wx + b$ 에서

$$\frac{\partial L}{\partial W} = \frac{\partial L}{\partial z}x^{\top}, \qquad \frac{\partial L}{\partial b} = \frac{\partial L}{\partial z} \;\;\Longrightarrow\;\; x = \frac{\partial L/\partial W[i,:]}{\partial L/\partial b[i]}$$

실측 결과:

| | 값 |
|---|---:|
| 복원 PSNR | **164.91 dB** |
| 최대 화소 오차 | **2.4 × 10⁻⁷** (float32 한계) |
| 라벨 복원 (300장) | **300 / 300** |

**최초 출처는 Phong 외, IEEE TIFS 13(5):1333–1345, 2018 — §3 Example 1, 식 (7)–(8), 관찰 (O1) 입니다.**
Geiping·Fowl·Boenisch 에 최초 귀속시키면 틀립니다(Geiping 외 NeurIPS 2020 명제 3.1 은 스스로를
"Phong 외의 일반화"라고 밝힙니다).

### 그리고 방어 두 개가 무너집니다

| 시도 | 실측 | 판정 |
|---|---|---|
| 배치 32로 키운다 | 개별 표본 대비 12.4 dB — 성공처럼 보인다. 그러나 **δ 가중평균 대비 151.81 dB** | 대상이 바뀔 뿐 |
| 클리핑 $C$=0.01 (배율 0.00096) | **165.83 dB** | **방어력 정확히 0** |
| 노이즈 $\sigma$=0.01 | 10.68 dB | 유일하게 듣는다 |

클리핑이 무력한 이유는 수식에 있습니다 — 나눗셈에서 배율이 약분됩니다.

---

## 이번 주차의 정점 — 막는 것과 보장하는 것

| $\sigma$ | 복원 | $\varepsilon$ ($\delta$=10⁻⁵) | 정확도 | 손실 |
|---:|---|---:|---:|---:|
| 0 | 완전 복원 (158 dB) | 보장 없음 | 89.40% | — |
| **0.01** | 파괴 (10.7 dB) | **1,422,867** | 89.50% | **없음** |
| 1.0 | 파괴 | 9.83 | 88.35% | 1.05%p |
| **2.0** | 파괴 | **3.27** | 85.90% | **3.50%p** |

> **공격을 막는 것은 공짜였습니다. 보장을 얻는 것은 3.50%p 였습니다.**
> $e^{1422867}$ 은 아무것도 제약하지 않습니다.

DP 실험은 **표본 단위 클리핑**(`torch.func.vmap`)을 씁니다. 배치 기울기를 자르면 DP-SGD 가 아니고
형식적 보장도 없습니다 — Abadi 외(CCS 2016) §3.1 이 이 대비를 명시합니다.

---

## 인용 검증에서 바로잡은 것

| 흔히 도는 말 | 실제 |
|---|---|
| "FedAvg 는 통신을 10~100배 줄인다" | MNIST CNN **IID** 34.8배, **병리적 비-IID 최고 2.8배**, 일부 설정은 **0.5배로 오히려 느림**. CIFAR-10 의 64.3배는 FedSGD 가 아니라 **중앙집중 SGD 대비**(FedSGD 대비 4.8배)이고 **IID 전용** |
| "비-IID 면 55% 떨어진다" | KWS·클래스 1개 분할·작은 배치 조건. **MNIST 는 6.5~11.3%**. 출처는 **프리프린트** |
| "디리클레 분할은 Hsu 외가 제안" | 최초는 **Yurochkin 외, ICML 2019**(α=0.5). Hsu 외는 **프리프린트** |
| "DP-SGD 는 ε=2 에서 MNIST 97%" | (2, 10⁻⁵) → **95%**. 97%는 **ε=8**. 모델도 CNN 이 아니라 **DP-PCA(60) + 은닉층 1000 FFN** |
| "Apple 은 ε=1~2 로 DP" | **1회 기부당** 값. 독립 분석은 macOS 10.12.3 **하루 허용 손실 합계 16**, 예산 매일 갱신이라 보고(프리프린트) |
| "DLG 는 표준 ResNet 에서 동작" | 논문이 **ReLU→Sigmoid 교체, stride 제거**를 명시. **배치 ≤ 8, 해상도 ≤ 64×64** |
| "iDLG 는 배치에서도 라벨 복원" | **배치 1** · 교차엔트로피 + 원-핫 조건. 그리고 **프리프린트** |
| "배치를 키우면 복원 불가능" | 배치 100에서도 일부 이미지가 육안 식별 가능. **"배치로 인한 왜곡은 균일하지 않다"**(Geiping 외 §6) |
| "보안 집계가 역복원을 막는다" | 악의적 서버는 **참여자 수·프로토콜과 무관하게 우회**(Pasquini 외, CCS 2022) |
| Kairouz 외를 "프리프린트"로 | **정식 출판**된 monograph — *Found. Trends Mach. Learn.* 14(1–2):1–210, 2021 |

논문 제목 주의 — Pasquini 외의 정확한 제목은 *"Eluding Secure Aggregation in Federated Learning
**via Model Inconsistency**"* 입니다. 그리고 Boenisch 외(EuroS&P 2023)는 **보안 집계를 깬 논문이
아닙니다** — 저자들 스스로 그 공을 Pasquini 에게 돌립니다.

---

## 규제 (2.5 · 그림 11)

| 구분 | 법적 지위 | 근거 |
|---|---|---|
| **가명정보** | **여전히 개인정보다** | 개인정보 보호법 제2조 제1호 **다목**, 제28조의2 |
| **익명정보** | **법 적용 제외** | 제58조의2 |

제58조의2 전문: *"이 법은 시간ㆍ비용ㆍ기술 등을 합리적으로 고려할 때 다른 정보를 사용하여도
더 이상 개인을 알아볼 수 없는 정보에는 적용하지 아니한다."*

**연합 학습 업데이트가 어느 쪽인지에 대한 개인정보보호위원회의 확정된 유권해석은 없습니다.**
다만 2.1 에서 기울기로부터 원본이 해석적으로 복원되므로, 연합 학습 그 자체만으로 제58조의2
요건을 충족한다고 보기는 어렵습니다. **"사안별로 재식별 가능성을 평가해야 한다"** 가 방어 가능한
유일한 서술입니다.

GDPR 도 같습니다 — EDPB 의견 28/2024(2024.12.18) ¶31 은 학습 데이터가 모델 파라미터에
**"흡수(absorbed)"** 되어 남을 수 있다고 판단했습니다. 다만 이 의견서는 **연합 학습을 직접
다루지 않으므로** "EDPB 가 연합 학습에 대해 판단했다"고 말하면 안 됩니다.

---

## 강의 운영 (가장 중요한 부분입니다)

**2.1 과 3.2 는 순서가 전부입니다.**

1. 칠판에 **두 줄만** 씁니다 — $\partial L/\partial W = \delta x^{\top}$, $\partial L/\partial b = \delta$
2. **5분 기다립니다.** 반에서 누군가 "나누면 되는데요?" 라고 말합니다.
3. **그때** 복원 이미지를 엽니다.

학생이 스스로 나눗셈을 떠올린 뒤에 그림을 보면 효과가 완전히 다릅니다.

**2.2 의 두 반박("배치를 키우면?", "클리핑하면?")은 학생에게서 나오게 하십시오.** 자기가 제안한
방어가 실측으로 깨지는 경험이 설명보다 열 배 오래 갑니다. 특히 클리핑 표는 **$C$=0.01 줄만 가리고**
보여 주다가 여는 것이 좋습니다.

**2.3 의 $\varepsilon$ = 1,422,867 은 칠판에 그대로 쓰십시오.** 그리고 "$e$ 의 142만 제곱이 무엇을
제약합니까?" 라고 물으십시오. 이 한 줄이 이번 주차 전체를 요약합니다.

**3.4 까지 반드시 가십시오.** 여기서 멈추면 "연합 학습은 위험하다"로 끝나 버리고, 그것은 절반만
맞는 결론입니다.

---

## 정직한 한계 (교재에도 그대로 적어 두었습니다)

1. 역복원 실험의 모델은 **첫 층이 원본 입력을 그대로 받는 MLP** 입니다. 합성곱 신경망은 더 어렵습니다.
   **다만 어려운 것과 불가능한 것은 다릅니다**(3교시 선택 과제 A).
2. 우리 공격은 **정직한 서버**를 가정합니다. 악의적 서버는 훨씬 강합니다.
3. $\varepsilon$ 은 **클라이언트 로컬 DP** 기준입니다($\delta=10^{-5}$, 표본 비율 0.0794, 260 스텝).
4. **MNIST 는 비-IID 로 잘 안 무너집니다.** 이것은 실험의 실패가 아니라 정보입니다.
5. 절대값은 재실행 시 흔들립니다(복원 PSNR **153~168 dB**). 재현을 요구할 것은 **구조**입니다 —
   나눗셈 한 번으로 완전히 복원된다 · 클리핑은 무력하다 · 노이즈만 듣는다 · 그 노이즈의 $\varepsilon$ 은 보장이 아니다.
