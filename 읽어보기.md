# 8주차 교재 개편본 — 사용법

## 들어 있는 것

```
pages/          w08-0-overview / w08-1-intro / w08-2-advanced / w08-3-lab  (전면 재작성)
assets/         w08_*.png 13장 (전부 새로 제작, 구 그림 6장은 삭제 대상)
TOC.md          8주차 제목 3개가 갱신된 최신본
README.md       저장소 안내 (8주차 ✅ 반영)
sync_toc_py.txt → sync_toc.py 로 이름을 바꿔서 쓰십시오 (.py 다운로드가 막히는 경우가 있어 .txt 로 넣었습니다)
lab08/          이 주차의 모든 숫자를 만든 실험 코드와 결과 JSON
```

## 저장소에 반영하는 순서

```bash
# 1. 페이지·그림·TOC·README 를 덮어씁니다
cp pages/*.md   <저장소>/pages/
cp assets/*.png <저장소>/assets/
cp TOC.md README.md <저장소>/

# 2. 구 8주차 그림 6장을 지웁니다 (더 이상 참조되지 않습니다)
cd <저장소>/assets
rm -f w08_p1_tinyml_spectrum_01.png w08_p1_mcu_constraints_02.png w08_p1_sw_stack_03.png \
      w08_p2_duty_cycling_04.png w08_p2_depthwise_05.png w08_p3_deploy_06.png

# 3. 배포 전 무결성 검사 (통과해야 push)
cd <저장소> && python3 sync_toc.py

# 4. push
git add -A && git commit -m "docs: 08주차 개편 (TinyML 실측 기반)" && git push origin main
```

## lab08 재현

```bash
pip install torch torchvision onnx cairosvg
python3 run_mem.py      # 메모리 실험 → mem.json   (약 2분)
python3 run_grid.py     # 예산 격자   → grid.json  (약 20초)
python3 run_energy.py   # 전력 산술   → energy.json (즉시)
python3 student.py      # 3교시 학생용 40줄 (교재의 출력과 동일해야 합니다)
python3 make_figs.py    # 그림 13장 재생성
```

`student.py` 는 3교시 교재에 실린 코드를 그대로 이어 붙인 것입니다. 실행하면 교재의 출력이 한 글자도 안 틀리고 나와야 합니다.

## 이번 주차의 검증 상태

- **인용 20건 독립 검증 완료.** 검증 과정에서 아래를 바로잡았습니다.
  - MCUNet 저자에서 자주 빠지는 John Cohn 을 포함했습니다(저자 6명, Lin 이 둘).
  - 널리 인용되는 "메모리 4.8배 감소"는 arXiv 메타데이터의 잔재입니다. 논문 본문은 **3.4배** 입니다.
  - "활성값이 병목"이라는 주장은 MCUNet 이 아니라 **MCUNetV2** 의 것입니다. 교재는 그렇게 귀속했습니다.
  - MCUNetV2 의 학회 게재 제목에는 "MCUNetV2:" 접두어가 없습니다.
  - MLPerf Tiny **논문 본문의 키워드 인식 파라미터 38.6K 는 참조 모델의 값이 아닙니다**(선행 연구의 값). 교재는 MLCommons 참조 구현에서 직접 센 값을 씁니다.
  - MLPerf Tiny 의 참조 플랫폼은 STM32F746 이 아니라 **NUCLEO-L4R5ZI** 입니다. 혼동하는 자료가 많습니다.
- **하드웨어 수치 5건 데이터시트 대조 완료.** 초안에서 쓴 nRF52840 값 두 개가 조건이 틀려 바로잡았습니다.
  - 활성 6.3 mA(LDO) → **3.3 mA**(DC/DC). 같은 표의 다른 줄입니다.
  - 수면 1.5 µA(RAM 미유지) → **3.16 µA**(256 kB RAM 전체 유지 + RTC). 가중치를 램에 두고 자야 하므로 후자가 맞습니다.
  - 이 정정으로 손익분기 주기가 7.0분 → **1.74분** 으로 바뀌었습니다. 교재의 모든 전력 수치는 정정 후 값입니다.
- **그림 13장 시각 검토 2회 · 슬라이드 34장 시각 검토 2회.** 마지막 라운드 블로커 0건.
