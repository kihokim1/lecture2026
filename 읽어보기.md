# 10주차 교재 개편본 — 사용법

## 들어 있는 것

```
pages/          w10-0-overview / w10-1-intro / w10-2-advanced / w10-3-lab  (전면 재작성)
assets/         w10_*.png 12장 (전부 새로 제작, 구 그림 6장은 삭제 대상)
TOC.md          10주차 제목 4개가 갱신된 최신본
README.md       저장소 안내 (10주차 ✅ 반영)
sync_toc_py.txt → sync_toc.py 로 이름을 바꿔서 쓰십시오
lab10/          이 주차의 모든 숫자를 만든 실험 코드·결과 JSON
```

## 저장소에 반영하는 순서

```bash
cp pages/*.md   <저장소>/pages/
cp assets/*.png <저장소>/assets/
cp TOC.md README.md <저장소>/

# 구 10주차 그림 6장을 지웁니다 (더 이상 참조되지 않습니다)
cd <저장소>/assets
rm -f w10_p1_attention_bottleneck_01.png w10_p1_kv_cache_02.png \
      w10_p1_compact_bert_03.png w10_p2_quant_4bit_04.png \
      w10_p2_speculative_05.png w10_p3_bench_06.png

cd <저장소> && python3 sync_toc.py          # 통과해야 push
git add -A && git commit -m "docs: 10주차 개편 (프리필·디코드 실측)" && git push origin main
```

## lab10 재현

```bash
pip install torch transformers numpy cairosvg
python3 run_llm.py      # 프리필/디코드 분리 · 양자화 · 문맥 스윕 · KV · 배치 → llm.json   (약 6분)
python3 run_budget.py   # 토크나이저 · 읽기 속도 토큰 예산                   → budget.json (약 1분)
python3 student.py      # 3교시 학생용 코드 — 교재와 같은 구조가 나와야 합니다
python3 make_figs.py    # 그림 12장 재생성 (SVG → PNG)
```

모델은 `HuggingFaceTB/SmolLM2-135M-Instruct`(**Apache-2.0**)입니다. 9주차의 YOLO11(AGPL-3.0)과 달리
라이선스 제약이 없어 수업 자료·과제에 그대로 쓰실 수 있습니다. GPU 도 API 키도 필요 없습니다.

---

## 이번 개편에서 바로잡은 것 (인용 검증 결과)

| 흔히 도는 표현 | 실제 |
|---|---|
| "vLLM 은 24배 빠르다" | 논문(SOSP 2023)의 비교 대상은 Orca(Oracle) 이고 **2~4배**. 24배는 블로그의 HuggingFace 대비 값 |
| "Orca 는 연속 배칭으로 36.9배" | 논문에 **"continuous batching" 이라는 용어 자체가 없다**(반복 수준 스케줄링). 36.9배는 GPT-3 175B·FT 대비, 190 ms SLO 아래의 값 |
| "추측 디코딩은 2~3배" | T5-XXL·TPU-v4 기준. 온디바이스 이득은 **초안 모델 수용률**에 달렸다 |
| "GQA 가 3배 빠르다" | GQA 의 이득은 **KV 가 지배하는 구간**의 이야기다. 배치 1 온디바이스는 가중치가 지배한다 |
| "1 토큰 ≈ 0.75 단어" | OpenAI 토크나이저·영어 기준. 우리 실측은 **영어 0.87 · 한국어 0.12 단어/토큰** |
| LLM.int8() / GPTQ / AWQ 제목 | NeurIPS 게재본은 **"GPT3.int8()"**, ICLR 게재본은 **"OPTQ"**, AWQ 는 MLSys 판에만 "On-Device" 가 들어간다 |

구조 수치(층·헤드·어휘)는 **논문이 아니라 모델 카드/`config.json`** 에서 인용했습니다. SmolLM2 논문의
하이퍼파라미터 표는 1.7B 판의 것이라 135M 에 그대로 쓰면 틀립니다.

---

## 수치 일관성 안내

`llm.json` 안에는 프롬프트 128토큰 측정이 **두 군데** 있습니다.

- `split` — 1.1/1.2 의 **기준 실험**. TTFT 291.6 ms · 프리필 438.9 tok/s · TPOT 45.4 ms · 디코드 22.0 tok/s
- `ctx[1]` — 1.3 의 **문맥 길이 스윕** 중 128 지점. TTFT 246.5 ms · 프리필 519.3 tok/s

교재·슬라이드의 "20배"는 **`split` 하나만** 씁니다(438.9 / 22.0 = 19.9). 스윕 표(1.3)는 `ctx` 를 씁니다.
두 값이 15% 정도 다른 것이 이 실험의 **재현성 폭 그 자체**이므로, 학생에게는 절대값이 아니라
**"프리필이 한 자릿수 이상 빠르다"** 는 구조를 재현하게 하십시오.

---

## 이번 주차의 교육적 정점

5주차(조건부) → 9주차(**1.52배 느려짐**) → 10주차(**1.57배 빨라짐**). 같은 동적 INT8 양자화가
세 번 다른 결과를 냅니다. 세 결과를 **연산 강도** 하나로 설명하는 것이 이번 주의 목표입니다.

배치도 같은 구조입니다 — 9주차는 처리량 1.24배에 지연 6.5배(나쁜 거래), 10주차는 3.9배에 2.06배(좋은 거래).

한국어 토큰 예산(**단어당 7.5배, 읽기 속도 대비 0.64배 미달**)은 학생 반응이 가장 큰 지점입니다.
영어 줄만 먼저 보여 주고 "우리 수업 자료는 한국어인데 어떨까요?" 로 여십시오.
