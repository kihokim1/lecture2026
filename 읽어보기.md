# 11주차 교재 개편본 — 사용법

## 들어 있는 것

```
pages/          w11-0-overview / w11-1-intro / w11-2-advanced / w11-3-lab  (전면 재작성)
assets/         w11_*.png 12장 (전부 새로 제작, 구 그림 6장은 삭제 대상)
TOC.md          11주차 제목 4개가 갱신된 최신본
README.md       저장소 안내 (11주차 ✅ 반영)
sync_toc_py.txt → sync_toc.py 로 이름을 바꿔서 쓰십시오
lab11/          이 주차의 모든 숫자를 만든 실험 코드·결과 JSON
```

## 저장소에 반영하는 순서

```bash
cp pages/*.md   <저장소>/pages/
cp assets/*.png <저장소>/assets/
cp TOC.md README.md <저장소>/

# 구 11주차 그림 6장을 지웁니다 (더 이상 참조되지 않습니다)
cd <저장소>/assets
rm -f w11_p1_ai_compiler_01.png w11_p1_host_device_02.png \
      w11_p1_scratchpad_03.png w11_p2_tensorrt_04.png \
      w11_p2_npu_sdk_05.png w11_p3_profiling_06.png

cd <저장소> && python3 sync_toc.py          # 통과해야 push
git add -A && git commit -m "docs: 11주차 개편 (연산자 커버리지·그래프 분할 실측)" && git push origin main
```

## lab11 재현

```bash
pip install onnx onnxruntime numpy torch torchvision cairosvg pillow
python3 run_cover.py     # 커버리지 · 분할 · 왕복 · 커버리지 곡선  → cover.json   (약 4분)
python3 run_compile.py   # 융합 · 빌드 시간 · 최적화 레벨 비교     → compile.json (약 3분)
python3 run_boundary.py  # 대역폭 · 레이아웃 · INT8 · 손익분기     → boundary.json (약 3분)
python3 student.py       # 3교시 학생용 코드 (모델 경로를 인자로)
python3 make_figs.py     # 그림 12장 재생성 (SVG → PNG)
```

`run_cover.py` 는 ResNet-18 과 소형 Transformer 인코더를 직접 내보내 씁니다. MobileNetV2 는
8주차의 `mbv2.onnx`, YOLO11n 은 9주차의 `yolo11n_320.onnx` 를 그대로 재사용합니다 —
경로가 다르면 각 스크립트 상단의 `MODELS` 목록만 고치면 됩니다.

**가속기·GPU 가 전혀 필요 없습니다.** 이번 주차의 모든 숫자는 CPU 한 대에서 나왔습니다.

---

## 이 주차의 방법론 — 왜 가속기 없이 가능한가

가속기 이식의 성패를 정하는 세 숫자는 **가속기를 켜 보기 전에** 전부 계산할 수 있습니다.

| 숫자 | 어디서 나오나 |
|---|---|
| **노드 커버리지** | ONNX 그래프 + 허용 연산자 목록 |
| **시간 커버리지** | ONNX Runtime 의 `enable_profiling` — 노드별 실행 시간을 그냥 준다 |
| **왕복 횟수** | 위상 정렬 후 장치가 바뀌는 지점을 센다 |

여기에 실측 대역폭을 넣으면 손익분기까지 나옵니다. 3교시 `student.py` 가 이 전부를 여든 줄로 합니다.

**분할 정의** — 우리 분할기는 위상 정렬한 노드 목록을 순서대로 훑으며 장치가 바뀌는 횟수를 셉니다.
실제 파티셔너는 노드를 재정렬해 이보다 잘하며, **그 차이를 2.2 에서 직접 측정했습니다(최대 35%)**.

---

## 이번 개편에서 바로잡은 것 (인용 검증 결과)

| 흔히 도는 표현 | 실제 |
|---|---|
| "TVM 은 1.2~3.8배 빠르다" | **그런 범위는 논문에 없다.** 서버 GPU(Titan X) 대 프레임워크가 **1.6~3.8배**(§6.1), 모바일 GPU(Mali) 대 ARM Compute Library 가 **1.2~1.6배**(§6.3). 다른 하드웨어·다른 베이스라인의 끝값을 이어붙인 유령 수치다 |
| "Ansor 는 AutoTVM 보다 3.8배" | AutoTVM 대비는 **1.02~1.8배**. 초록의 "최대 3.8배"는 PyTorch·TensorFlow·TensorRT 등 여러 베이스라인 전체의 최대값이다 |
| "Triton 이 cuBLAS 보다 빠르다" | 논문 주장은 **"on par"(동등)** 이고 GTX 1070 기준. 그리고 2019년 MAPL 논문의 Triton 은 C 기반 언어이지 **지금의 Python DSL 이 아니다** |
| "Amdahl 이 1967년에 그 공식을 냈다" | 원논문(AFIPS vol. 30, pp. 483–485)에는 **수식이 하나도 없고 그림 한 장뿐**이다 |
| "ORT 는 최적화를 파티셔닝 전에 한다" | **Basic 만** 전에. **Extended·Layout 은 파티셔닝 후**, **CPU/CUDA/ROCm 노드에만** 적용된다 |
| "NNAPI 는 미지원 연산자를 CPU 로 폴백한다" | 문서 표현은 "**might** fall back" 이고, CPU 구현이 없으면 **실패**한다. NNAPI 는 **Android 15 에서 deprecated** 되었다 |
| Glow 를 "논문" 으로 인용 | **arXiv 프리프린트**(1805.00907). 동료심사 게재 이력 없음 |
| XLA 를 "(저자, 연도)" 로 인용 | **정경 논문이 없다.** 문서 URL + 접속일자로 인용해야 정확하다 |
| "separating algorithm from schedule" | 널리 쓰이는 **의역**이다. 원문은 *"decoupling the algorithm definition from its execution strategy"* (Halide, PLDI 2013 §1) |
| TensorRT 의 "vertical/horizontal fusion" | 오래된 NVIDIA **블로그** 표현이고 현행 공식 문서 용어가 아니다 |

**용어** — 이 분야의 표준어는 "AI 컴파일러"가 아니라 **딥러닝 컴파일러(deep learning compiler)** 이고,
융합은 **operator fusion** 입니다. NVIDIA 는 layer fusion, XLA 는 operation fusion 이라 부릅니다.

---

## 이번 주차의 교육적 정점

**2.1 의 MobileNetV2 두 줄**입니다.

> A형 가속기에 두 배 빠른 코어를 넣으면 **1.03배**.
> 같은 가속기가 `Clip` **하나만 더** 받아 주면 **2.00배**.

강의 때 **A형 줄만 먼저** 보여 주시고, 충분히 당황하게 둔 다음 B형 줄을 여십시오.
"칩을 두 배 빠르게 만드는 것보다 연산자 하나를 지원하는 것이 압도적으로 싸다" 가 이번 주의 실무적 결론입니다.

그리고 **1.3 의 왕복 비단조성**(`Sigmoid` 추가 → 왕복 141→147)은 반드시 먼저 예측시키십시오.
전원이 "줄어든다"고 답합니다. 그 오답이 2교시의 완벽한 도입입니다.

---

## 앞선 주차와의 연결

- **9주차의 미결 항목이 풀립니다** — 동적 INT8 이 YOLO11n 을 1.52배 느리게 만든 두 번째 이유가
  2.4 에서 드러납니다. **그래프가 320 → 838 노드(2.62배)로 불어났고**, 늘어난 518개는 계산이 아니라
  양자화·역양자화·재조정입니다.
- **암달이 세 번째로 등장합니다** — 9주차 분모는 파이프라인 단계, 10주차는 연산 강도,
  11주차는 **연산자 커버리지**. 그리고 여기서 처음으로 **분모 밖에 항이 하나 더 붙습니다**(왕복세).
- **4·5·6주차로 되돌아갑니다** — 폐쇄형 NPU 에서는 커널을 못 짜므로 **모델 쪽을 고치는 것이
  유일한 지렛대**입니다(2.6).

---

## 정직한 한계 (교재에도 그대로 적어 두었습니다)

1. 경계 비용 $c$ 에 **커널 실행·동기화 고정비가 빠져 있습니다.** 실제 가속기는 왕복마다 수십 µs 가
   더 붙으므로 우리 판정은 **낙관적인 쪽**입니다. 3교시 선택 과제 C 가 이것을 다룹니다.
2. 허용목록 A·B·C형은 **벤더 목록이 아니라 우리가 정의한 세대별 능력 등급**입니다.
3. CPU 에서 잰 op 별 시간 비중이 가속기에서도 같다는 보장은 없습니다. 재현을 요구하는 것은
   배수가 아니라 **"노드와 시간이 다르다 · 조각이 수십 개 난다 · 연산자 하나가 판정을 뒤집는다"** 는 구조입니다.
