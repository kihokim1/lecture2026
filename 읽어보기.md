# 9주차 교재 개편본 — 사용법

## 들어 있는 것

```
pages/          w09-0-overview / w09-1-intro / w09-2-advanced / w09-3-lab  (전면 재작성)
assets/         w09_*.png 12장 (전부 새로 제작, 구 그림 6장은 삭제 대상)
TOC.md          9주차 제목 4개가 갱신된 최신본
README.md       저장소 안내 (9주차 ✅ 반영)
sync_toc_py.txt → sync_toc.py 로 이름을 바꿔서 쓰십시오
lab09/          이 주차의 모든 숫자를 만든 실험 코드·결과 JSON·테스트 이미지
```

## 저장소에 반영하는 순서

```bash
cp pages/*.md   <저장소>/pages/
cp assets/*.png <저장소>/assets/
cp TOC.md README.md <저장소>/

# 구 9주차 그림 6장을 지웁니다 (더 이상 참조되지 않습니다)
cd <저장소>/assets
rm -f w09_p1_backbones_01.png w09_p1_detection_02.png w09_p1_nms_03.png \
      w09_p2_segmentation_04.png w09_p2_accel_sdk_05.png w09_p3_pipeline_06.png

cd <저장소> && python3 sync_toc.py          # 통과해야 push
git add -A && git commit -m "docs: 09주차 개편 (엣지 비전 파이프라인 실측)" && git push origin main
```

## lab09 재현

```bash
pip install onnxruntime numpy pillow opencv-python ultralytics cairosvg
python3 run_pipeline.py   # 단계별 시간 · 해상도 스윕 · 암달  → pipeline.json  (약 3분)
python3 run_realtime.py   # NMS 데이터 의존성 · 전처리 · 배치 → realtime.json  (약 2분)
python3 run_queue.py      # 큐 시뮬레이션 · NMS 구현 비교     → queue.json     (약 1분)
python3 student.py        # 3교시 학생용 코드 — 교재와 같은 구조가 나와야 합니다
python3 make_figs.py      # 그림 12장 재생성
```

`run_pipeline.py` 는 YOLO11n 을 해상도 5종으로 ONNX 내보내기 하므로 첫 실행에 시간이 걸립니다.

> **절대값이 아니라 구조를 보십시오.** 2코어 CPU 기준이며 재실행 시 ±10 % 흔들립니다. 이 문서를 만들며 두 번 돌린 전체 지연이 49.0 ms 와 53.6 ms 였습니다. 재현해야 하는 것은 **「추론 70 % 남짓 · 나머지 30 % 남짓 · 암달 상한 3.5배 근처 · 큐 발산」** 이라는 구조입니다.

## 이번 주차의 검증 상태

- **인용 19건 독립 검증 완료.** 검증에서 바로잡은 것:
  - **SSD 의 유명한 "59 FPS" 는 배치 8 의 값**입니다. 같은 논문 같은 표(Table 7)에 배치 1 값이 **46 FPS** 로 있습니다. 온디바이스 실시간 지각에서 의미 있는 값은 후자입니다.
  - **YOLO 의 63.4 mAP 는 COCO 가 아니라 PASCAL VOC 2007** 값입니다. 하드웨어는 Titan X, 배치 없음.
  - **Faster R-CNN 의 "17 fps" 는 ZF 넷**의 값이고 VGG-16 은 5 fps 입니다. 하드웨어는 Titan X 가 아니라 **Tesla K40**.
  - **DETR 은 NMS 가 느리다고 말한 적이 없습니다.** 논거는 손수 설계된 구성 요소 제거와 NMS 가 참 양성을 지운다는 것입니다. 속도 근거로 인용하려면 **YOLOv10 (NeurIPS 2024)** 을 써야 합니다.
  - **「Towards Streaming Perception」의 지표 이름은 "스트리밍 정확도"** 입니다. 흔히 쓰는 "sAP" 는 코드 저장소 이름입니다.
  - **RetinaNet(Focal Loss)은 ICCV 2017 최우수 *학생* 논문**입니다(Marr Prize 는 Mask R-CNN).
  - **MobileNetV3 초록의 "34% 빠르다"는 헤드 교체만의 효과가 아닙니다.** 백본 V2→V3 + 채널 반감 + 헤드 교체 셋이 섞여 있고, 헤드만 바꾸면 약 2 % 빨라지고 mIOU 가 0.4 오릅니다.
  - **COCO 의 작은/중간/큰 물체 기준(32², 96²)은 ECCV 2014 논문에 없습니다.** 평가 문서와 `cocoeval.py` 가 1차 출처이며, 넓이는 **분할 마스크의 픽셀 수**로 잽니다.
  - **암달의 1967년 원논문에는 수식이 하나도 없습니다.** "암달의 법칙" 공식은 후대의 정식화입니다.
  - **YOLO11 에는 심사를 거친 논문이 없습니다.** 소프트웨어로 인용해야 하며 라이선스는 **AGPL-3.0** 입니다 — 캡스톤 팀 주의사항을 개요 페이지에 넣었습니다.
- **그림 12장 시각 검토 3회 · 슬라이드 32장 시각 검토 2회.** 마지막 라운드 블로커 0건.
- 검토 과정에서 그림 두 개의 **축 설계 오류**를 잡았습니다 — NMS 비용 그림이 같은 단위(ms) 두 계열을 서로 다른 눈금으로 그려 메시지와 반대로 읽히던 것을 **공통 축**으로 고쳤고, 구현 비교 그림은 0.065~19.88 ms 를 선형으로 그려 작은 막대가 안 보이던 것을 **로그 축**으로 고쳤습니다.
