# -*- coding: utf-8 -*-
"""이 과목 4~12주차의 인용 검증에서 실제로 바로잡은 것들.

매주 교재를 쓰기 전에 독립 검토자가 1차 출처(논문 PDF·공식 문서·데이터시트·법령)를
대조했고, 그때 나온 정정을 전부 여기에 모았다. 13주차의 데이터셋이다.

유형 여섯 가지:
  조건   — 수치는 맞는데 하드웨어·데이터셋·배치·분할·버전 조건이 잘려 나갔다
  기준선 — 수치는 맞는데 비교 대상이 인용된 것과 다르다
  범위   — 서로 다른 실험의 값을 하나의 범위로 이어 붙였다
  서지   — 프리프린트를 게재 논문으로, 제목·게재처가 실제와 다르다
  귀속   — 다른 논문(또는 후속 논문)의 공을 잘못 돌렸다
  무주장 — 그 논문은 그런 말을 한 적이 없다
"""

# (주차, 유형, 흔히 도는 말, 실제)
CORPUS = [
    # ── 6주차 지식 증류 ─────────────────────────────────────────
    (6, "서지", "Hinton 증류는 NIPS 2014 논문이다",
     "NIPS 2014 딥러닝 **워크숍** 발표다. 학회 논문집에 실린 적이 없다"),
    (6, "조건", "증류는 온도를 올릴수록 좋다",
     "논문도 \"층당 30개짜리 작은 학생은 T=2.5~4가 뚜렷이 좋았다\"고 적었다. 작은 학생일수록 낮은 온도를 좋아한다"),

    # ── 8주차 TinyML ────────────────────────────────────────────
    (8, "조건", "nRF52840 활성 전류는 6.3 mA 다",
     "6.3 mA 는 **LDO** 값이다. DC/DC 를 쓰면 같은 표의 다른 줄이 3.3 mA 다"),
    (8, "조건", "nRF52840 수면 전류는 1.5 µA 다",
     "1.5 µA 는 **RAM 미유지** 값이다. 가중치를 램에 두고 자려면 3.16 µA 를 써야 한다"),
    (8, "서지", "MCUNet 은 메모리를 4.8배 줄였다",
     "arXiv 메타데이터의 잔재다. 논문 본문은 **3.4배**(vs TFLM/CMSIS-NN)이고 3.5·5.7배는 기준선이 다른 별개 수치다"),
    (8, "귀속", "MCUNet 이 \"활성값이 병목\"이라고 했다",
     "**MCUNetV2** 의 주장이다. MCUNet v1 은 SRAM↔활성 / Flash↔가중치 구분만 한다"),
    (8, "서지", "MCUNetV2 논문 제목에 \"MCUNetV2:\" 가 붙는다",
     "NeurIPS 게재 제목에는 그 접두어가 없다"),
    (8, "귀속", "MLPerf Tiny 의 DS-CNN 은 38.6K 파라미터다",
     "그 숫자는 인용된 선행 연구(Hello Edge)의 값이다. 참조 모델은 약 24.9K 다"),
    (8, "조건", "MLPerf Tiny 참조 플랫폼은 STM32F746 이다",
     "**NUCLEO-L4R5ZI**(Cortex-M4F)다. STM32F746 은 MCUNet·CMSIS-NN 의 보드다"),
    (8, "기준선", "CMSIS-NN 은 TFLM 대비 4.6배 빠르다",
     "기준선은 TFLM 이 아니라 **손으로 짠 CMSIS-DSP 구현**이다"),
    (8, "조건", "MCU 에서 지연은 연산 수에 선형이다 (MicroNets)",
     "조건이 넷이다 — 레이어 단위에서는 성립 안 함 · 백본마다 기울기 다름 · TFLM+CMSIS-NN·Cortex-M4/M7 한정 · \"op\"은 MAC 이 아니다(1 MAC = 2 op)"),

    # ── 9주차 시각 지능 ─────────────────────────────────────────
    (9, "조건", "SSD 는 59 FPS 다", "**배치 8** 의 값이다. 배치 1 은 46 FPS 다"),
    (9, "조건", "YOLO 의 mAP 는 COCO 기준이다", "**VOC07** 기준이다"),
    (9, "조건", "Faster R-CNN 은 17 fps 다", "**ZF 백본**의 값이다. VGG-16 은 5 fps 다"),
    (9, "무주장", "DETR 은 NMS 가 느리다고 지적했다",
     "논문은 그런 주장을 하지 않는다. NMS 제거의 근거는 속도가 아니라 **파이프라인의 단순화**다"),
    (9, "조건", "MobileNetV3 는 34% 빨라졌다", "**세 가지 변경을 묶은** 값이다"),
    (9, "무주장", "COCO 의 IoU 임계값 정의가 ECCV 논문에 있다", "그 논문에 없다"),
    (9, "무주장", "Amdahl 이 1967년에 그 공식을 제시했다",
     "원논문(AFIPS vol.30, pp.483–485)에 **수식이 하나도 없고 그림 한 장뿐**이다"),
    (9, "서지", "YOLO11 논문을 인용한다", "YOLO11 에는 논문이 없다. 그리고 **AGPL-3.0** 이다"),

    # ── 10주차 언어 지능 ────────────────────────────────────────
    (10, "기준선", "vLLM 은 24배 빠르다",
     "논문(SOSP 2023)의 비교 대상은 Orca 이고 **2~4배**다. 24배는 블로그의 HuggingFace 대비 값이다"),
    (10, "무주장", "Orca 는 연속 배칭(continuous batching)을 제안했다",
     "논문에 **그 용어 자체가 없다**. 논문 용어는 반복 수준 스케줄링(iteration-level scheduling)이다"),
    (10, "서지", "LLM.int8() 로 인용한다",
     "NeurIPS 게재본 제목은 **\"GPT3.int8()\"** 이다"),
    (10, "서지", "GPTQ 로 인용한다", "ICLR 게재본 제목은 **\"OPTQ\"** 다"),
    (10, "조건", "GQA 는 3배 빠르다",
     "GQA 의 이득은 **KV 가 지배하는 구간**의 이야기다. 배치 1 온디바이스는 가중치가 지배한다"),
    (10, "서지", "Shazeer 의 MQA 논문", "학회 발표가 없다. arXiv 프리프린트다"),
    (10, "서지", "Chen 외의 추측 표집 논문", "학회 발표가 없다. arXiv 프리프린트다"),
    (10, "조건", "1 토큰 ≈ 0.75 단어",
     "**OpenAI 토크나이저·영어** 기준이다. 우리 실측은 영어 0.87 · 한국어 0.12 단어/토큰이다"),

    # ── 11주차 가속기 ───────────────────────────────────────────
    (11, "범위", "TVM 은 1.2~3.8배 빠르다",
     "**그런 범위는 논문에 없다.** 서버 GPU 대 프레임워크 1.6~3.8배(§6.1)와 모바일 GPU 대 ACL 1.2~1.6배(§6.3)의 끝값을 이어 붙인 것이다"),
    (11, "기준선", "Ansor 는 AutoTVM 보다 최대 3.8배 빠르다",
     "AutoTVM 대비는 **1.02~1.8배**다. 3.8배는 PyTorch·TF·TensorRT 등 여러 베이스라인 전체의 최대값이다"),
    (11, "무주장", "Triton 이 cuBLAS 보다 빠르다",
     "논문 주장은 **\"on par\"(동등)** 이고 GTX 1070 기준이다. 게다가 그 Triton 은 지금의 Python DSL 이 아니다"),
    (11, "조건", "ONNX Runtime 은 그래프 최적화를 파티셔닝 전에 한다",
     "**Basic 만** 전에 돈다. Extended·Layout 은 파티셔닝 **후**, CPU/CUDA/ROCm 노드에만 적용된다"),
    (11, "조건", "NNAPI 는 미지원 연산자를 CPU 로 폴백한다",
     "문서 표현은 \"**might** fall back\"이고, CPU 구현이 없으면 폴백 대신 **실패**한다. 그리고 Android 15 에서 deprecated 되었다"),
    (11, "서지", "Glow 논문을 인용한다", "**arXiv 프리프린트**(1805.00907)이고 동료심사 게재 이력이 없다"),
    (11, "서지", "XLA 를 (저자, 연도)로 인용한다",
     "**정경 논문이 없다.** 문서 URL + 접속일자로 인용해야 정확하다"),
    (11, "서지", "Halide 의 \"separating algorithm from schedule\"",
     "널리 쓰이는 **의역**이다. 원문은 \"decoupling the algorithm definition from its execution strategy\"(PLDI 2013 §1)다"),
    (11, "서지", "TensorRT 의 vertical/horizontal fusion",
     "오래된 **블로그** 표현이고 현행 공식 문서 용어가 아니다"),
    (11, "조건", "가속기의 peak TOPS 만큼 빨라진다",
     "Edge TPU 실측은 이론 최대보다 **평균 75.6% 낮고**, Transducer·LSTM 은 **1% 미만**이다"),

    # ── 12주차 연합 학습·프라이버시 ─────────────────────────────
    (12, "조건", "FedAvg 는 통신 라운드를 10~100배 줄인다",
     "MNIST CNN **IID** 는 34.8배지만 **병리적 비-IID 는 최고 2.8배**이고 일부 설정은 **0.5배로 오히려 느리다**"),
    (12, "기준선", "FedAvg 는 CIFAR-10 에서 64.3배 줄였다",
     "FedSGD 가 아니라 **중앙집중 SGD 대비**다. FedSGD 대비는 4.8배이고, 그 실험은 **IID 전용**이다"),
    (12, "조건", "비-IID 면 정확도가 55% 떨어진다",
     "**KWS 데이터셋·기기당 클래스 1개·작은 배치** 조건의 값이다. 같은 논문의 MNIST 는 6.5~11.3% 다"),
    (12, "귀속", "디리클레 분할은 Hsu 외가 제안했다",
     "최초는 **Yurochkin 외, ICML 2019**(α=0.5 고정)다. Hsu 외는 α 를 연속 범위로 확장했고 프리프린트다"),
    (12, "조건", "FedProx 가 FedAvg 를 22% 앞선다", "**탈락률 90%** 설정에서의 평균값이다"),
    (12, "조건", "DP-SGD 는 ε=2 에서 MNIST 97% 다",
     "(2, 10⁻⁵) 는 **95%** 이고, 97%는 **ε=8** 이다"),
    (12, "조건", "DP-SGD 의 MNIST 결과는 CNN 이다",
     "**DP-PCA(60차원) + 은닉층 1000유닛 FFN** 이다"),
    (12, "조건", "DP-SGD 는 CIFAR-10 을 73%까지 올렸다",
     "**CIFAR-100 을 공개 데이터로 삼아 합성곱층을 사전학습하고 FC 층만 DP 로 학습**한 결과다"),
    (12, "조건", "Apple 은 ε=1~2 로 DP 를 적용한다",
     "그것은 **1회 기부당** 값이다. 독립 분석은 macOS 10.12.3 의 **하루 허용 손실 합계가 16**이라고 보고했다"),
    (12, "조건", "DLG 는 표준 ResNet 에서 동작한다",
     "논문이 **ReLU 를 Sigmoid 로 바꾸고 stride 를 제거**했다고 밝힌다"),
    (12, "조건", "DLG 는 고해상도 이미지를 복원한다",
     "논문 자신이 적은 한계는 **배치 ≤ 8, 해상도 ≤ 64×64** 다"),
    (12, "조건", "iDLG 는 배치에서도 라벨을 복원한다",
     "**배치 1** · 교차엔트로피 + 원-핫 라벨 조건에서 성립한다"),
    (12, "무주장", "배치를 키우면 개별 표본은 복원 불가능하다",
     "배치 100에서도 일부 이미지가 육안 식별 가능하다. \"배치로 인한 왜곡은 **균일하지 않다**\""),
    (12, "무주장", "보안 집계를 쓰면 기울기 역복원이 막힌다",
     "악의적 서버는 **참여자 수·프로토콜과 무관하게** 우회한다(Pasquini 외, CCS 2022)"),
    (12, "귀속", "선형층 해석적 역산은 Geiping·Fowl·Boenisch 의 결과다",
     "최초는 **Phong 외, IEEE TIFS 13(5), 2018 §3 Example 1 관찰 (O1)** 이다. Geiping 외 스스로 \"Phong 외의 일반화\"라고 밝힌다"),
    (12, "서지", "Kairouz 외의 FL 서베이는 프리프린트다",
     "**정식 출판**된 monograph 다 — Found. Trends Mach. Learn. 14(1–2):1–210, 2021"),
    (12, "서지", "Pasquini 외의 제목은 \"…is Easy\" 다",
     "정확한 제목은 \"Eluding Secure Aggregation in Federated Learning **via Model Inconsistency**\" 다"),
    (12, "귀속", "Boenisch 외가 보안 집계를 깼다",
     "그 논문은 **보안 집계를 깬 논문이 아니다.** 저자들 스스로 그 공을 Pasquini 외에게 돌린다"),
    (12, "서지", "Zhao 외 비-IID 논문 / Hsu 외 / Hard 외 / Tang 외 / iDLG",
     "**전부 프리프린트**다. 학회·저널 게재 이력이 없다"),
]

TYPES = ["조건", "기준선", "범위", "서지", "귀속", "무주장"]

TYPE_DESC = {
    "조건": "수치는 맞는데 하드웨어·데이터셋·배치·분할·버전 조건이 잘려 나갔다",
    "기준선": "수치는 맞는데 비교 대상이 인용된 것과 다르다",
    "범위": "서로 다른 실험의 값을 하나의 범위로 이어 붙였다",
    "서지": "프리프린트를 게재 논문으로, 또는 제목·게재처가 실제와 다르다",
    "귀속": "다른 논문(또는 후속 논문)의 공을 잘못 돌렸다",
    "무주장": "그 논문은 그런 말을 한 적이 없다",
}

# 그 정정이 실제로 결론을 바꾼 사례 (숫자만 고친 것이 아니라)
CHANGED_CONCLUSION = [
    (8, "nRF52840 조건 정정으로 **손익분기 주기가 7.0분 → 1.74분**으로 바뀌었다. "
        "듀티 사이클 설계 전체가 달라진다"),
    (11, "\"TVM 1.2~3.8배\"를 조건별로 분리하자 **모바일에서는 1.2~1.6배**임이 드러났다. "
         "가속기 도입 판단이 달라진다"),
    (12, "\"비-IID 55%\"를 조건과 함께 읽자 **MNIST 는 6.5~11.3%**였다. "
         "우리 실측 7.05%p 가 이상값이 아니라 정상값이 된다"),
    (12, "선형층 역산의 귀속을 바로잡자 **최초 출처가 2018년 TIFS 논문**이 되었다. "
         "\"최근에 발견된 공격\"이 아니라 **8년 된 알려진 결과**다"),
]
